# AGENTS.md

本文件适用于仓库根目录及所有子目录，供在本项目中工作的编码代理使用。

## 项目概览

这是一个基于 Electron、React、TypeScript 和 Vite 的直播中控工具。

- `src/`：渲染进程，包括页面、组件、Hooks、路由和前端状态。
- `electron/main/`：Electron 主进程、平台自动化、任务、服务和 IPC 处理。
- `electron/preload/`：预加载脚本，负责向渲染进程暴露受控 API。
- `shared/`：主进程、预加载脚本和渲染进程共用的类型与 IPC 通道。
- `scripts/`：诊断、发布和辅助脚本。
- `dist/`、`dist-electron/`、`release/`：生成产物，不要手工编辑。

## 开发原则

- 先阅读相关实现及调用链，再修改代码。
- 保持改动范围最小，不顺带重构无关模块。
- 优先沿用现有组件、Hooks、状态管理和错误处理模式。
- 不覆盖、还原或格式化与当前任务无关的用户改动。
- 除非需求明确要求，否则不要改变现有持久化数据语义。
- 新增依赖前先确认现有依赖或平台 API 无法满足需求。
- 不提交密钥、令牌、账号信息或本地环境配置。

## 用户工作习惯

以下规则为本仓库的硬性约束，除非用户在当前任务中明确覆盖：

- 不生成、补充或修改测试代码。
- 不运行任何测试命令。
- 不创建 Git 提交。
- 不创建或切换 Git 分支。
- 不创建 Git worktree，直接在当前工作目录中完成修改。

## Electron 与 IPC

- 渲染进程不能直接使用 Node.js 或 Electron 主进程 API。
- 新增或修改 IPC 时，检查并同步以下位置：
  - `shared/ipcChannels.ts`
  - `shared/electron-api.d.ts`
  - `electron/preload/index.ts`
  - 对应的 `electron/main/ipc/` 处理器
  - 渲染进程调用方
- IPC 参数和返回值必须使用明确的 TypeScript 类型。
- 平台页面自动化应放在 `electron/main/platforms/`，任务编排应放在
  `electron/main/tasks/`，不要将浏览器操作塞进 React 组件。
- 主进程错误需要记录足够上下文，同时向界面返回可理解的失败结果。

## React 与状态

- 页面放在 `src/pages/`，可复用界面放在 `src/components/`，跨页面逻辑放在
  `src/hooks/` 或 `src/utils/`。
- 使用现有 Zustand store 管理共享状态；局部交互状态保留在组件内。
- 持久化 store 只保存恢复页面所需的数据，不持久化运行中标记、请求句柄或计时器。
- 默认值应集中定义并在创建状态、缺省读取等入口复用，避免重复魔法数字。
- 异步操作必须处理重复启动、停止、组件卸载和过期响应。
- 用户可见文案使用简洁中文，并与当前界面用词保持一致。

## 代码风格

- 遵循 `biome.json`：单引号、无分号、100 字符行宽、尾随逗号。
- 保持 TypeScript 严格类型，不用 `any` 绕过类型检查。
- 使用现有路径别名：`@/`、`#/` 和 `shared/`。
- 优先使用小而明确的函数；只在逻辑不直观时添加简短注释。
- 不直接修改生成文件，也不要提交仅由本地运行产生的临时文件。

## 验证

默认不执行命令。只有用户明确要求时，才选择适合改动范围的非测试验证：

```bash
# 类型检查
npx tsc --noEmit

# 格式和静态检查指定文件
npx biome check path/to/file.ts

# 完整构建和打包
npm run build
```

- 不生成测试代码，也不运行测试。
- 未经用户要求，不运行类型检查、静态检查或完整构建。
- 不得声称检查或构建通过，除非本轮实际运行并确认命令成功。

## Git 与交付

- 开始修改前检查 `git status`，区分当前任务和用户已有改动。
- 不使用 `git reset --hard`、`git checkout --` 或其他会丢失工作区内容的命令。
- 不创建提交、分支或 worktree，不推送代码，也不创建 Pull Request。
- 交付时简要说明修改文件、行为变化、已执行的验证，以及未验证的部分。
