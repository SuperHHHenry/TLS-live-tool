# 自动开播联动已有中控流程

用户已确认：不改变原来的逻辑，只新增自动入口。

确认自动开播成功后，调用与“连接直播控制台”按钮相同的连接流程。连接使用当前账号和现有平台、浏览器配置；后续平台判断、8 秒等待、自动弹窗、福袋、回复、观众进场及评论轮换全部保持原样。不额外启动其他功能。

全局监听开播成功状态，切换页面后仍能触发。同一次开播不重复连接，已连接或正在连接时不重复触发。原定时开关播服务不修改。

静态检查：开播确认前不连接；确认后触发一次；非断开状态不重复连接；现有启动顺序及失败处理不变。用户要求不写、不运行测试；仅执行代码差异审查和类型检查。
